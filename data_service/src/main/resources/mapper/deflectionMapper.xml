<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.nero.click.data.dao.DeflectionDao">

    <!--新的mapper-->
    <select id="getDel" resultType="org.nero.click.data.entity.DGene">
        SELECT id,ucStart,pvalue${cancerType} as pvalue,men${cancerType} as men,met${cancerType} as met from ${dataType}_gene_xiugai where ucChrom = ${chroosome} ;
    </select>


</mapper>